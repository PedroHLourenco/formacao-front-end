// desafio 1: criar uma função que aceita dois argumentos, somente do tipo numérico (number) e exibir a soma entre os dois;

const num1 = 5;
const num2 = 10;

function sum(num1: number, num2: number) {
  return num1 + num2;
}

console.log(sum(num1, num2));
